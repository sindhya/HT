
void form_tour(struct edgeHashTable *edgeHash,struct tourVertex *tourVertices,struct edge *tourEdges);

void optimize_tour(struct edgeHashTable *edgeHash,struct edge* tourEdges,struct tourVertex* tourVertices);

void optimize_weight(struct edgeHashTable *edgeHash,struct edge* tourEdges,struct tourVertex* tourVertices);
